[project]
name = "leaf-guard-apis"
version = "1.0.1"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.115.0",
  "uvicorn[standard]>=0.30.0",
  "python-multipart>=0.0.9",
  "pillow>=10.0.0",
  "numpy>=1.26.0,<2.3.0",
  "pydantic>=2.0.0",
  "ai-edge-litert>=1.2.0",
]

[tool.vercel]
entrypoint = "app.main:app"
